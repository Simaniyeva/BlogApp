@{
    ViewData["Title"] = "Index";
}
@model IDataResult<List<UserGetDto>>
<div class="content-header">
    <div class="row">
        <div class="col-xl-12 col-lg-12">
            <div class="card mb-4">
                <header class="card-header">
                    <div>
                        <h2 class="content-title card-title">Admins </h2>
                        <p>Edit, revoke or evoke admin</p>
                    </div>
                </header> <!-- card-header end// -->
                <div class="card-body">
                    <div class="table-responsive">
                        <partial name="_userTablePartial" model='Model.Data.Where(x=>x.Roles.Contains("Admin") == true).ToList()' />

                        <!-- table-responsive.// -->
                    </div>
                </div> <!-- card-body end// -->
            </div>
        </div>
        <div class="col-xl-12 col-lg-12">
            <div class="card mb-4">
                <header class="card-header">
                    <div>
                        <h2 class="content-title card-title">Users</h2>
                        <p>Edit, revoke, or evoke a user</p>
                    </div>
                </header> <!-- card-header end// -->
                <div class="card-body">
                    <div class="table-responsive">
                        <partial name="_userTablePartial" model='Model.Data.Where(x=>x.Roles.Contains("User") == true).ToList()' />

                        <!-- table-responsive.// -->
                    </div>
                </div> <!-- card-body end// -->
            </div>
        </div>
    </div>
</div>


